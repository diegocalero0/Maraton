#include<bits/stdc++.h>
using namespace std;

int main(){

	int a, b, c;
    char aux, aux2;
	cin >> a >> aux >> b >> aux2 >> c;
	if(a+b == c)
	   cout << "YES" << endl;
	else
	   cout << "NO" << endl;
	return 0;
}