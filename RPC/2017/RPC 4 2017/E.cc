#include<bits/stdc++.h>
using namespace std;
#define debug(x) cout << #x << " = " << x << endl;
int main(){

	int t, k;
	cin >> t;
	int n, ai, arr[100000];
	while(t--){
		cin >> n >> k;
		for(int i = 0; i < n; i++)
			cin >> arr[i];
		cout << "Yes." << endl;
	}

	return 0;
}