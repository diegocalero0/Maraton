#include<bits/stdc++.h>

using namespace std;

int main(){

    int n;
    cin >> n;
    int operations[n];
    int numeros[n];
    string o;
    int num;
    int i = 0;
    while(n--){
        cin >> o;
        cin >> num;

        if()

        switch(str2int(o)){
        case "ADD":
            operations[i] = 1;
            break;
        case "SUBTRACT":
            operations[i] = 1;
            break;
        case "MULTIPLY":
            operations[i] = 1;
            break;
        case "DIVIDE":
            operations[i] = 1;
            break;
        }

        numeros[i++] = num;

    }


    return 0;
}
